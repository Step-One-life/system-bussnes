import type { SubscriptionType } from '../enums'

/**
 * Маппинг типа абонемента → число занятий. Используется и на фронте (при
 * планировании рекуррентной серии), и на бэке (при создании подписки в
 * `SubscriptionsService.add`). Единственный источник правды, чтобы
 * добавление нового типа абонемента требовало правки только здесь.
 */
export const SUB_TYPE_TOTALS: Record<SubscriptionType, number> = {
  '1': 1,
  '4': 4,
  '8': 8,
  '1_90': 1,
  '4_90': 4,
  '8_90': 8,
}
