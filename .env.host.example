# Параметры удалённого хоста для деплоя (make deploy-server).
# Скопируй в .env.host и заполни. Файл .env.host в .gitignore.

# SSH-доступ
HOST_SSH_USER=root
HOST_SSH_HOST=94.19.63.62
HOST_SSH_PORT=5678
# Пароль для sshpass (доступ по паролю)
HOST_SSH_PASS=

# Домен и каталог проекта на хосте
HOST_DOMAIN=trick.ozma-split.com
HOST_PROJECT_DIR=/home/ozma_split/sites/trick.ozma-split.com
